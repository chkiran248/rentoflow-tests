{
  "name": "rentoflow-tests",
  "version": "1.0.0",
  "description": "E2E automated tests for RentoFlow",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0"
  }
}
